import React from 'react';
import Box from '../../../../../shared/components/Box';

const Blog = () => {
  return <Box flex={1} height={245} marginTop={2} backgroundColor="blue" />;
};

export default Blog;

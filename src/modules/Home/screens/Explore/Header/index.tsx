import React from 'react';
import Box from '../../../../../shared/components/Box';

const Header = () => {
  return <Box flex={1} height={86} marginTop={2} backgroundColor="blue" />;
};

export default Header;

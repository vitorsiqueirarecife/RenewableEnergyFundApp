import React from 'react';
import Box from '../../../../../shared/components/Box';

const Summary = () => {
  return <Box flex={1} height={100} marginTop={2} backgroundColor="blue" />;
};

export default Summary;

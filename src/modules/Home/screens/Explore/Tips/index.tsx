import React from 'react';
import Box from '../../../../../shared/components/Box';

const Tips = () => {
  return <Box flex={1} height={125} marginTop={2} backgroundColor="blue" />;
};

export default Tips;

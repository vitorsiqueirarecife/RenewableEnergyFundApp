import {
  DisplayProps,
  FlexboxProps,
  PaddingProps,
  BorderProps,
  PositionProps,
  WidthProps,
  ColorProps,
} from 'styled-system';

export interface ButtonBaseProps
  extends DisplayProps,
    FlexboxProps,
    PaddingProps,
    BorderProps,
    PositionProps,
    WidthProps,
    ColorProps {}
